<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Authentication</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .form-container {
            max-width: 400px;
            margin: 0 auto;
            padding: 2rem;
        }
    </style>
</head>
<body class="bg-gray-900 flex items-center justify-center min-h-screen">
    <!-- Main Container -->
    <div class="form-container bg-zinc-800 rounded-lg shadow-lg text-white">

        <h2 class="text-center text-3xl font-semibold mb-6">Login Account</h2>
        
        <!-- Form Section -->
        <form method="POST" action="/login" class="space-y-4">
            <!-- Email Input -->
            <div>
                <input class="w-full px-4 py-2 rounded-md bg-transparent border-2 border-zinc-700 text-white focus:outline-none focus:ring-2 focus:ring-blue-500" 
                    type="email" name="email" placeholder="Email" required>
            </div>
            

            <!-- Password Input -->
            <div>
                <input class="w-full px-4 py-2 rounded-md bg-transparent border-2 border-zinc-700 text-white focus:outline-none focus:ring-2 focus:ring-blue-500" 
                    type="password" name="password" placeholder="Password" required>
            </div>

            <!-- Submit Button -->
            <div>
                <input class="w-full px-4 py-2 rounded-md bg-blue-500 text-white font-semibold cursor-pointer hover:bg-blue-600 transition duration-300" 
                    type="submit" value="login">
            </div>
        </form>

       

    </div>
</body>
</html>
